<Window x:Class="NewCustomerWindow.EmployeeList"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NewCustomerWindow"
        mc:Ignorable="d"
        Title="EmployeeList" Height="450" Width="800">

    <Grid Background="#f2f2f2" Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Employee List"
                   FontSize="24"
                   FontWeight="Bold"
                   Foreground="#2a4d69"
                   Margin="10" />

        <!-- Buttons -->
        <StackPanel Orientation="Horizontal"
                    Grid.Row="1"
                    HorizontalAlignment="Right"
                    Margin="10">
            <Button Content="← Back to Home"
                    Width="120"
                    Margin="0 0 10 0"
                    Background="#007bff"
                    Foreground="White"
                    Click="BackToHome_Click"/>
            <Button Content="+ Create New Employee"
                    Width="160"
                    Background="#28a745"
                    Foreground="White"
                    Click="CreateEmployee_Click"/>
        </StackPanel>

        <!-- Search Box -->
        <Grid Grid.Row="1" Margin="10 0 0 0" Width="200" HorizontalAlignment="Left">
            <TextBox x:Name="SearchBox"
                     Height="28"
                     VerticalContentAlignment="Center"
                     TextChanged="SearchBox_TextChanged"/>
            <TextBlock x:Name="SearchPlaceholder"
                       Text="Search..."
                       Foreground="Gray"
                       Margin="5,0,0,0"
                       VerticalAlignment="Center"
                       IsHitTestVisible="False"
                       Visibility="Collapsed"/>
        </Grid>

        <!-- DataGrid -->
        <DataGrid x:Name="EmployeeGrid"
                  Grid.Row="2"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  Margin="10"
                  Background="White"
                  HeadersVisibility="Column"
                  IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="*"/>
                <DataGridTextColumn Header="Department" Binding="{Binding Department}" Width="*"/>
                <DataGridTextColumn Header="Mobile" Binding="{Binding Mobile}" Width="*"/>

                <DataGridTemplateColumn Header="Actions" Width="160">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <Button Content="Edit"
                                        Width="60"
                                        Margin="0,0,5,0"
                                        Background="#007bff"
                                        Foreground="White"
                                        Click="EditButton_Click"/>
                                <Button Content="Delete"
                                        Width="60"
                                        Background="#dc3545"
                                        Foreground="White"
                                        Click="DeleteButton_Click"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Pagination -->
        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    Margin="10">
            <Button Content="← Prev"
                    Margin="5"
                    Width="70"
                    Click="PrevPage_Click"/>
            <TextBlock x:Name="PageText"
                       VerticalAlignment="Center"
                       Text="Page 1 of 1"
                       FontWeight="Bold"
                       Margin="5"/>
            <Button Content="Next →"
                    Margin="5"
                    Width="70"
                    Click="NextPage_Click"/>
        </StackPanel>
    </Grid>
</Window>
