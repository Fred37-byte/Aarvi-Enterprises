<Window x:Class="NewCustomerWindow.xaml.AddSocietyCarWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NewCustomerWindow.xaml"
        mc:Ignorable="d"
        Title="Add New Society" 
        Height="700" Width="550"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5">

    <Window.Resources>
        <!-- Input Style -->
        <Style x:Key="InputStyle" TargetType="TextBox">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#DDD"/>
            <Setter Property="Background" Value="White"/>
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="#4CAF50"/>
                    <Setter Property="Effect">
                        <Setter.Value>
                            <DropShadowEffect Color="#4CAF50" BlurRadius="5" Opacity="0.3" ShadowDepth="0"/>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- TextArea Style -->
        <Style x:Key="TextAreaStyle" TargetType="TextBox">
            <Setter Property="Height" Value="80"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#DDD"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="AcceptsReturn" Value="True"/>
            <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="#4CAF50"/>
                    <Setter Property="Effect">
                        <Setter.Value>
                            <DropShadowEffect Color="#4CAF50" BlurRadius="5" Opacity="0.3" ShadowDepth="0"/>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Label Style -->
        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#555"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
        </Style>

        <!-- Button Styles -->
        <Style x:Key="CancelButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="44"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Background" Value="#6C757D"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#5A6268"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SubmitButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="44"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Background" Value="#28A745"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#218838"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Error Text Style -->
        <Style x:Key="ErrorTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="Red"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Margin" Value="0,5,0,0"/>
            <Setter Property="Visibility" Value="Collapsed"/>
        </Style>

        <!-- Success Message Style -->
        <Style x:Key="SuccessMessageStyle" TargetType="Border">
            <Setter Property="Background" Value="#D4EDDA"/>
            <Setter Property="BorderBrush" Value="#C3E6CB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="4"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Margin" Value="0,0,0,20"/>
            <Setter Property="Visibility" Value="Collapsed"/>
        </Style>
    </Window.Resources>

    <!-- Main Container -->
    <Border Background="White" CornerRadius="8" Margin="20" Padding="30">
        <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="10" Opacity="0.1" ShadowDepth="2"/>
        </Border.Effect>

        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- Success Message -->
                <Border x:Name="SuccessMessage" Style="{StaticResource SuccessMessageStyle}">
                    <TextBlock Text="Society added successfully!" 
                               Foreground="#155724" 
                               FontWeight="SemiBold"/>
                </Border>

                <!-- Title -->
                <TextBlock Text="Add New Society" 
                           FontSize="24" 
                           FontWeight="Bold"
                           Foreground="#333" 
                           HorizontalAlignment="Center"
                           Margin="0,0,0,30"/>

                <!-- Society Name -->
                <StackPanel Margin="0,0,0,20">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Society Name" Style="{StaticResource LabelStyle}"/>
                        <TextBlock Text=" *" Foreground="Red" FontWeight="Bold"/>
                    </StackPanel>
                    <TextBox x:Name="txtSocietyName" 
                             Style="{StaticResource InputStyle}"
                             Tag="Enter society name"
                             GotFocus="TextBox_GotFocus"
                             LostFocus="TextBox_LostFocus"
                             TextChanged="TextBox_TextChanged"/>
                    <TextBlock x:Name="errorSocietyName" 
                               Text="Please enter society name" 
                               Style="{StaticResource ErrorTextStyle}"/>
                </StackPanel>

                <!-- Address -->
                <StackPanel Margin="0,0,0,20">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Address" Style="{StaticResource LabelStyle}"/>
                        <TextBlock Text=" *" Foreground="Red" FontWeight="Bold"/>
                    </StackPanel>
                    <TextBox x:Name="txtAddress" 
                             Style="{StaticResource TextAreaStyle}"
                             Tag="Enter complete address"
                             GotFocus="TextBox_GotFocus"
                             LostFocus="TextBox_LostFocus"
                             TextChanged="TextBox_TextChanged"/>
                    <TextBlock x:Name="errorAddress" 
                               Text="Please enter address" 
                               Style="{StaticResource ErrorTextStyle}"/>
                </StackPanel>

                <!-- Contact Number -->
                <StackPanel Margin="0,0,0,20">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Contact Number" Style="{StaticResource LabelStyle}"/>
                        <TextBlock Text=" *" Foreground="Red" FontWeight="Bold"/>
                    </StackPanel>
                    <TextBox x:Name="txtContactNumber" 
                             Style="{StaticResource InputStyle}"
                             Tag="Enter 10-digit number"
                             MaxLength="10"
                             PreviewTextInput="NumericOnly_PreviewTextInput"
                             GotFocus="TextBox_GotFocus"
                             LostFocus="TextBox_LostFocus"
                             TextChanged="TextBox_TextChanged"/>
                    <TextBlock x:Name="errorContactNumber" 
                               Text="Please enter valid 10-digit number" 
                               Style="{StaticResource ErrorTextStyle}"/>
                </StackPanel>

                <!-- Manager Name -->
                <StackPanel Margin="0,0,0,20">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Manager Name" Style="{StaticResource LabelStyle}"/>
                        <TextBlock Text=" *" Foreground="Red" FontWeight="Bold"/>
                    </StackPanel>
                    <TextBox x:Name="txtContactPerson" 
                             Style="{StaticResource InputStyle}"
                             Tag="Enter manager name"
                             GotFocus="TextBox_GotFocus"
                             LostFocus="TextBox_LostFocus"
                             TextChanged="TextBox_TextChanged"/>
                    <TextBlock x:Name="errorManagerName" 
                               Text="Please enter manager name" 
                               Style="{StaticResource ErrorTextStyle}"/>
                </StackPanel>

                <!-- Total Cars -->
                <StackPanel Margin="0,0,0,30">
                    <TextBlock Text="Total Cars" Style="{StaticResource LabelStyle}"/>
                    <TextBox x:Name="txtTotalCars" 
                             Style="{StaticResource InputStyle}"
                             Tag="Enter number of cars"
                             PreviewTextInput="NumericOnly_PreviewTextInput"
                             GotFocus="TextBox_GotFocus"
                             LostFocus="TextBox_LostFocus"/>
                </StackPanel>

                <!-- Buttons -->
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Button Grid.Column="0"
                            Content="Clear" 
                            Style="{StaticResource CancelButtonStyle}"
                            Click="BtnClear_Click"/>

                    <Button Grid.Column="2"
                            Content="Add Society" 
                            Style="{StaticResource SubmitButtonStyle}"
                            Click="BtnSave_Click"/>
                </Grid>
            </StackPanel>
        </ScrollViewer>
    </Border>
</Window>